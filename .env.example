# Database Connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/projectpulse

# Server Configuration
PORT=7000
NODE_ENV=development

# JWT Secret (replace this with a strong secret in production)
JWT_SECRET=your_jwt_secret_key_here

# LDAP Configuration (for authentication)
LDAP_URL=ldap://localhost:389
LDAP_BIND_DN=cn=admin,dc=example,dc=com
LDAP_BIND_PASSWORD=admin
LDAP_SEARCH_BASE=ou=users,dc=example,dc=com
LDAP_SEARCH_FILTER=(uid={{username}})

# Optional Configuration
# DEFAULT_PASSWORD=ProjectPulse@2023
# SIGNAL_CORPS_DEPT_ID=5c01b20e-f22d-4dd2-9d75-75b7bfe480e5 